#include<stdio.h>

main() {
	int N;
	scanf("%d", &N);

	for (int i = 0; i < N; i++) {
		for (int z = 0; z < i; z++)
			printf(" ");
		for (int j = 1; j <= (N - i) * 2 - 1; j++)
			printf("*");
		printf("\n");
	}
}
