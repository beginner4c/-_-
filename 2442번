#include<stdio.h>

main() {
	int N;
	scanf("%d", &N);

	for (int i = 0; i < N; i++) {
		for (int j = N - i; j > 1; j--)
			printf(" ");
		for (int z = 1; z <= (i + 1) * 2 - 1; z++)
			printf("*");
		printf("\n");
	}
}
