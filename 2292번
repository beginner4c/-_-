#include<stdio.h>

main() {
	int N;
	scanf("%d", &N);

	int count = 1;
	int width = 1;

	while (1) {
		if (width >= N) {
			printf("%d", count);
			return;
		}
		width += (6 * count);
		++count;
	}

}
